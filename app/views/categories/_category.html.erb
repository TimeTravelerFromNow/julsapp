<div id="<%= dom_id category %>" class="category">

  <div class="title h3" >
    <%= category.name %>
  </div>

  <% if category.category_image.present? %>
    <%= image_tag(category.category_image , class: 'image') %>
  <% end %>
</div>
